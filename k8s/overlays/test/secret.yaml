# 로컬 전용 더미 자격증명 (docker-compose.test.yml 과 동일 값). 운영 비밀 아님.
apiVersion: v1
kind: Secret
metadata:
  name: commerce-secret
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "root"
  MYSQL_USER: "commerce"
  MYSQL_PASSWORD: "commerce"
  MAILGUN_APIKEY: "dummy-key"
  MAILGUN_DOMAIN: "dummy.local"

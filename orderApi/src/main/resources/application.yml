server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/order?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: commerce
    password: commerce

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379